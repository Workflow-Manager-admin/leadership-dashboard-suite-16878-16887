# Environment configuration for dashboard_frontend (React)
# Copy this file to '.env' in this same folder. Do NOT commit your actual .env file with secrets.

# The backend API endpoint for the React app to use. Required for API calls.
REACT_APP_API_URL=http://localhost:3001

# CAUTION: Only set this to 'true' if you need to disable host checking (dev environments with non-localhost URLs, like codespaces).
REACT_APP_DANGEROUSLY_DISABLE_HOST_CHECK=false

# For preview/cloud/container/remote dev servers:
# To fix "Invalid Host header" errors (common in cloud IDE, Codespaces, Gitpod, or port-forwarded/preview links),
# uncomment the line below (remove #) to make the dev server accessible from any host:
# HOST=0.0.0.0

# After setting HOST, RESTART the dev server for this to have effect.
# For details: https://create-react-app.dev/docs/advanced-configuration/#custom-environment-variables

