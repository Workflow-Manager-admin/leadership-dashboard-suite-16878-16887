# Example environment variables for dashboard_frontend

# Allows all hosts in React DevServer (for preview/CLOUD builds only!)
DANGEROUSLY_DISABLE_HOST_CHECK=true

# Backend API base URL (update for deployment)
REACT_APP_API_URL=http://localhost:3001
